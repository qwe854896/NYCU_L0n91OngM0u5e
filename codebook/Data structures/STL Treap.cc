/*
#include <ext/rope> //header with rope
using namespace __gnu_cxx; //namespace with rope and some additional stuff

rope<int> x, y; int s, len;  // nsqrt(n)

x.size(),    x.push_back(s),    x.substr(pos, s)

x.insert(pos, s or y)
x.erase(pos, len)  delete [pos, pos + len)
x.replace(pos, s) x[pos] = s
x.copy(pos, len, y) copy [pos, pos + len) to y
*/